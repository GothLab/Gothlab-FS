
/*
 $(document).ready(function() {
    var sizeRange = $('#size-range');

    sizeRange.on('input', function() {
      var value = $(this).attr('value');
console.log(value)
      // Use Bootstrap Tooltip API to update content
     // $(this).tooltip('dispose').attr('data-bs-original-title', value).tooltip('show');
    });

    // Initialize Bootstrap Tooltip
    sizeRange.tooltip({
      trigger: 'manual'
    });
  });
  */